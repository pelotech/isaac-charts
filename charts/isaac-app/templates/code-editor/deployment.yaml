apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "isaac-app.codeEditor.fullname" . }}
  labels:
    {{- include "isaac-app.labels" . | nindent 4 }}
    {{- include "isaac-app.codeEditor.selectorLabels" . | nindent 4 }}
spec:
  replicas: {{ .Values.codeEditor.replicaCount }}
  selector:
    matchLabels:
      {{- include "isaac-app.codeEditor.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.codeEditor.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "isaac-app.codeEditor.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.codeEditor.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.codeEditor.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.codeEditor.securityContext | nindent 12 }}
          image: "{{ .Values.codeEditor.image.repository }}:{{ .Values.codeEditor.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.codeEditor.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
{{/*reenable*/}}
{{/*          livenessProbe:*/}}
{{/*            httpGet:*/}}
{{/*              path: /*/}}
{{/*              port: http*/}}
{{/*          readinessProbe:*/}}
{{/*            httpGet:*/}}
{{/*              path: /*/}}
{{/*              port: http*/}}
          resources:
            {{- toYaml .Values.codeEditor.resources | nindent 12 }}

      {{- with .Values.codeEditor.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.codeEditor.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.codeEditor.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}