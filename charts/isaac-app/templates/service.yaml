apiVersion: v1
kind: Service
metadata:
  name: {{ include "isaac-app.fullname" . }}
  labels:
    {{- include "isaac-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "isaac-app.selectorLabels" . | nindent 4 }}
